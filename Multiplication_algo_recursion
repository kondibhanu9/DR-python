def fun(a,b):
        if a==1:
                return b
        if a%2:
                return b+fun(a//2,b*2)
        else:
                return fun(a//2,b*2)
a,b=map(int,input().split())
print(fun(a,b))
